@model Storage.Models.ViewModels.ProductSummaryViewModel

@{
    ViewData["Title"] = "Summary";
}

<h1>Summary</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductSummary.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductSummary.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductSummary.Count)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductSummary.InventoryValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ProductSummaries)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Count)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InventoryValue)
            </td>
        </tr>
}
        <tr>
            <td>
                Total inventory value:
            </td>
            <td>
            </td>
            <td>
            </td>
            <td>
                @Html.DisplayFor(Model => Model.TotalInventoryValue)
            </td>
        </tr>
    </tbody>
</table>
